/**
 * Class to extend to create specific resorces.
 * Resources should extend {@link Commodity} and their types should be placed
 * in {@link CommoditiesTypes} enum.
 */
export default abstract class Resource {

}